[build]
  command = "npm run build"
  publish = "dist"

# SPA redirect — required for React Router to work on page refresh / direct URL access
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
